{
  "name": "monorepo-project",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "start": "npm run start -w system-app",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
